// Namespaces are defined in the Root.fusion file

prototype(Page).body.javascripts.outdatedBrowser = Fusion:Array {
	Markup = OB:Markup
	CSS = OB:CSS
	Javascript = OB:Javascript
	@position = 'start 1'
	@if.isLive = ${node.context.live}
}
